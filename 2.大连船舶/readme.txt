插件描述：在背景平面图上创建和编辑区块，包括区块描述和颜色，支持背景拖拽移动和区块拖拽移动。

根据项目需要写的这个小插件，暂时比较简易，只实现基本功能。

1、添加区块：设置区块描述，拖拽移动位置和控制尺寸；

2、编辑区块：邮件点击区块弹出菜单，可编辑和删除区块，并可输入尺寸改变区块大小，设置区块颜色；

3、锁定区块：画布过大时可锁定区块，锁定后拖动只会移动画布（背景平面图）；

4、获取区块数据：获取区块相关数据，包括位置、描述、大小、颜色，并拼装JSON数据格式；

5、加载区块：根据JSON数据加载区块到画布；

使用到的工具类：jquery，layer，context。